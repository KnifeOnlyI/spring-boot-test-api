spring:
  datasource:
    url: jdbc:h2:mem:test
    username: test
    password: test

  liquibase:
    change-log: classpath:/db/changelog-test/master.xml

  jpa:
    show-sql: true

jwt:
  secret: the-secret-jwt
